<Window x:Class="VintageStoryModManager.Views.Dialogs.CloudModlistManagementDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage Cloud Modlists"
        SizeToContent="WidthAndHeight"
        MinWidth="520"
        MinHeight="340"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanMinimize"
        WindowStyle="SingleBorderWindow"
        ShowInTaskbar="False"
        Background="{StaticResource Window_Background_Brush}">
    <Grid Margin="20"
          Background="{StaticResource Panel_Primary_Background_Brush}"
          TextElement.Foreground="{StaticResource Text_Primary_Brush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Select one of your cloud modlists to rename or delete it."
                   Grid.Row="0"
                   Margin="0,0,0,12"
                   TextWrapping="Wrap"
                   FontSize="14" />

        <ListView x:Name="ModlistsListView"
                  Grid.Row="1"
                  MinHeight="220"
                  Width="460"
                  HorizontalAlignment="Stretch"
                  SelectionMode="Single"
                  SelectionChanged="ModlistsListView_OnSelectionChanged"
                  MouseDoubleClick="ModlistsListView_OnMouseDoubleClick"
                  Background="{StaticResource Panel_Secondary_Background_Brush}"
                  BorderBrush="{StaticResource Panel_Secondary_Background_Brush}"
                  Foreground="{StaticResource Text_Primary_Brush}">
            <ListView.Resources>
                <Style TargetType="GridViewColumnHeader">
                    <Setter Property="Foreground" Value="{StaticResource Text_Primary_Brush}" />
                    <Setter Property="Background" Value="{StaticResource Button_General_Background_Brush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource Panel_Secondary_Background_Brush}" />
                </Style>
                <Style TargetType="ListViewItem">
                    <Setter Property="Foreground" Value="{StaticResource Text_Primary_Brush}" />
                </Style>
            </ListView.Resources>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Slot" DisplayMemberBinding="{Binding SlotLabel}" Width="100" />
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding EffectiveName}" Width="240" />
                    <GridViewColumn Header="Version" DisplayMemberBinding="{Binding Version}" Width="100" />
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,18,0,0">
            <Button x:Name="RenameButton"
                    Content="Rename"
                    MinWidth="88"
                    Margin="0,0,12,0"
                    Click="RenameButton_OnClick" />
            <Button x:Name="DeleteButton"
                    Content="Delete"
                    MinWidth="88"
                    Margin="0,0,12,0"
                    Background="{StaticResource Status_Error_Brush}"
                    Foreground="{StaticResource Text_Primary_Brush}"
                    Click="DeleteButton_OnClick" />
            <Button Content="Close"
                    MinWidth="88"
                    IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
