<Window x:Class="VintageStoryModManager.Views.Dialogs.CloudModlistManagementDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage Cloud Modlists"
        SizeToContent="WidthAndHeight"
        MinWidth="520"
        MinHeight="340"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanMinimize"
        WindowStyle="SingleBorderWindow"
        ShowInTaskbar="False"
        Background="{StaticResource IMM.WindowBackgroundBrush}">
    <Grid Margin="20"
          Background="{StaticResource IMM.SurfaceBrush}"
          TextElement.Foreground="{StaticResource IMM.ForegroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Select one of your cloud modlists to rename or delete it."
                   Grid.Row="0"
                   Margin="0,0,0,12"
                   TextWrapping="Wrap"
                   FontSize="14" />

        <ListView x:Name="ModlistsListView"
                  Grid.Row="1"
                  MinHeight="220"
                  Width="460"
                  HorizontalAlignment="Stretch"
                  SelectionMode="Single"
                  SelectionChanged="ModlistsListView_OnSelectionChanged"
                  MouseDoubleClick="ModlistsListView_OnMouseDoubleClick"
                  Background="{StaticResource IMM.DataGridBackgroundBrush}"
                  BorderBrush="{StaticResource IMM.BorderBrush}"
                  Foreground="{StaticResource IMM.ForegroundBrush}">
            <ListView.Resources>
                <Style TargetType="GridViewColumnHeader">
                    <Setter Property="Foreground" Value="{StaticResource IMM.ForegroundBrush}" />
                    <Setter Property="Background" Value="{StaticResource IMM.SurfaceAltBrush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource IMM.BorderBrush}" />
                </Style>
                <Style TargetType="ListViewItem">
                    <Setter Property="Foreground" Value="{StaticResource IMM.ForegroundBrush}" />
                </Style>
            </ListView.Resources>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Slot" DisplayMemberBinding="{Binding SlotLabel}" Width="100" />
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding EffectiveName}" Width="240" />
                    <GridViewColumn Header="Version" DisplayMemberBinding="{Binding Version}" Width="100" />
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,18,0,0">
            <Button x:Name="RenameButton"
                    Content="Rename"
                    MinWidth="88"
                    Margin="0,0,12,0"
                    Click="RenameButton_OnClick" />
            <Button x:Name="DeleteButton"
                    Content="Delete"
                    MinWidth="88"
                    Margin="0,0,12,0"
                    Click="DeleteButton_OnClick" />
            <Button Content="Close"
                    MinWidth="88"
                    IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
