<Window x:Class="VintageStoryModManager.Views.Dialogs.SelectConfigFileDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Config File"
        Width="420"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        MinWidth="320">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Select a configuration file to edit:" FontWeight="SemiBold" />

        <ListBox x:Name="ConfigFilesListBox"
                 Grid.Row="1"
                 ItemsSource="{Binding ConfigFiles}"
                 SelectedItem="{Binding SelectedConfigFile, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 HorizontalContentAlignment="Stretch"
                 MinHeight="200"
                 Margin="0,8,0,0"
                 SelectionChanged="ConfigFilesListBox_OnSelectionChanged"
                 MouseDoubleClick="ConfigFilesListBox_OnMouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="0,4">
                        <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding FilePath}" FontSize="11" Foreground="Gray" TextWrapping="Wrap" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Choose Files..." Margin="0,0,8,0" Click="ChooseFilesButton_OnClick" />
            <Button x:Name="EditButton" Content="Edit" IsDefault="True" Margin="0,0,8,0" Click="EditButton_OnClick" />
            <Button Content="Cancel" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
