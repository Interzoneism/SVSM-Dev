<Window x:Class="VintageStoryModManager.Views.Dialogs.CloudModlistRenameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rename Cloud Modlist"
        SizeToContent="WidthAndHeight"
        MinWidth="360"
        MinHeight="180"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        ShowInTaskbar="False"
        Loaded="Window_OnLoaded"
        Background="{StaticResource IMM.WindowBackgroundBrush}">
    <Grid Margin="20"
          Background="{StaticResource IMM.SurfaceBrush}"
          TextElement.Foreground="{StaticResource IMM.ForegroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Enter a new name for the cloud modlist."
                   Grid.Row="0"
                   Margin="0,0,0,12"
                   TextWrapping="Wrap" />

        <TextBox x:Name="NameTextBox"
                 Grid.Row="1"
                 MinWidth="220"
                 Margin="0,0,0,12"
                 TextChanged="NameTextBox_OnTextChanged"
                 Background="{StaticResource IMM.SurfaceAltBrush}"
                 BorderBrush="{StaticResource IMM.ButtonBorderBrush}"
                 SelectionBrush="{StaticResource IMM.AccentBrush}"
                 SelectionTextBrush="{StaticResource IMM.SelectionForegroundBrush}"
                 CaretBrush="{StaticResource IMM.AccentBrush}" />

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,8,0,0">
            <Button x:Name="ConfirmButton"
                    Content="Save"
                    MinWidth="88"
                    Margin="0,0,12,0"
                    IsDefault="True"
                    Click="ConfirmButton_OnClick" />
            <Button Content="Cancel"
                    MinWidth="88"
                    IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
