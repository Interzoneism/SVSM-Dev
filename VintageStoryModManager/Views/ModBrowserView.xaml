<UserControl x:Class="VintageStoryModManager.Views.ModBrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:VintageStoryModManager.Converters"
             xmlns:vm="clr-namespace:VintageStoryModManager.ViewModels"
             xmlns:views="clr-namespace:VintageStoryModManager.Views"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="#18181B"
             Loaded="OnLoaded">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        <converters:NullToVisibilityConverter x:Key="NullToVisibility"/>
        <converters:SelectedToBorderBrushConverter x:Key="SelectedToBorder"/>
        <converters:EqualityConverter x:Key="EqualityConverter"/>
        <converters:CountToVisibilityConverter x:Key="CountToVisibility"/>
        <converters:ContainsConverter x:Key="ContainsConverter"/>
        <converters:BooleanToBrushConverter x:Key="BoolToBrush"/>
        <converters:IconKeyToGeometryConverter x:Key="IconKeyToGeometry"/>
        <converters:NoResultsVisibilityConverter x:Key="NoResultsVisibility"/>
        <converters:FavoriteButtonVisibilityConverter x:Key="FavoriteButtonVisibility"/>

        <!-- Dark Theme Colors -->
        <SolidColorBrush x:Key="BackgroundBrush" Color="#18181B"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#27272A"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#3F3F46"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#FAFAFA"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#A1A1AA"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#3B82F6"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#22C55E"/>
        <SolidColorBrush x:Key="WarningBrush" Color="#EAB308"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="#EF4444"/>

        <!-- TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#09090B"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="CaretBrush" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="SelectionBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1A1A1D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Button Style -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#09090B"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#27272A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3F3F46"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Icon Button Style (for icon-only buttons with reduced padding) -->
        <Style x:Key="IconButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Padding" Value="4"/>
        </Style>

        <!-- ComboBox Toggle Button Template -->
        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="20"/>
                </Grid.ColumnDefinitions>
                <Border x:Name="Border"
                        Grid.ColumnSpan="2"
                        Background="#09090B"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="4"/>
                <Border Grid.Column="0"
                        Margin="1"
                        Background="Transparent"
                        CornerRadius="4,0,0,4"/>
                <Path x:Name="Arrow"
                      Grid.Column="1"
                      Fill="{StaticResource TextSecondaryBrush}"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      Data="M0,0 L4,4 L8,0"
                      RenderTransformOrigin="0.5,0.5">
                    <Path.RenderTransform>
                        <RotateTransform Angle="0"/>
                    </Path.RenderTransform>
                </Path>
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="Border" Property="Background" Value="#27272A"/>
                </Trigger>
                <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="Arrow" Property="RenderTransform">
                        <Setter.Value>
                            <RotateTransform Angle="180"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <!-- ComboBox TextBox Template -->
        <ControlTemplate x:Key="ComboBoxTextBox" TargetType="TextBox">
            <Border x:Name="PART_ContentHost" Focusable="False" Background="Transparent"/>
        </ControlTemplate>

        <!-- ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#09090B"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Template="{StaticResource ComboBoxToggleButton}"
                                          Grid.Column="2"
                                          Focusable="False"
                                          ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>
                            <ContentPresenter x:Name="ContentSite"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="8,3,28,3"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>
                            <TextBox x:Name="PART_EditableTextBox"
                                     Style="{x:Null}"
                                     Template="{StaticResource ComboBoxTextBox}"
                                     HorizontalAlignment="Left"
                                     VerticalAlignment="Center"
                                     Margin="8,3,28,3"
                                     Focusable="True"
                                     Background="Transparent"
                                     Foreground="{StaticResource TextPrimaryBrush}"
                                     CaretBrush="{StaticResource TextPrimaryBrush}"
                                     Visibility="Hidden"
                                     IsReadOnly="{TemplateBinding IsReadOnly}"/>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid x:Name="DropDown"
                                      SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                            Background="#09090B"
                                            BorderBrush="{StaticResource BorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="4"
                                            Margin="0,4,0,0"/>
                                    <ScrollViewer Margin="4,8,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="95"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsGrouping" Value="True">
                                <Setter Property="ScrollViewer.CanContentScroll" Value="False"/>
                            </Trigger>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter Property="IsTabStop" Value="False"/>
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBoxItem Style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                CornerRadius="2">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#27272A"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3B82F6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ScrollBar Thumb Style -->
        <Style x:Key="ScrollBarThumb" TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border Background="#52525B"
                                CornerRadius="4"
                                Margin="2"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ScrollBar Line Button Style -->
        <Style x:Key="ScrollBarLineButton" TargetType="RepeatButton">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Setter Property="Height" Value="0"/>
            <Setter Property="Width" Value="0"/>
        </Style>

        <!-- ScrollBar Page Button Style -->
        <Style x:Key="ScrollBarPageButton" TargetType="RepeatButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="Transparent"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Vertical ScrollBar Style -->
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid x:Name="GridRoot" Background="Transparent">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageUpCommand"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollBarThumb}"/>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageDownCommand"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="Height" Value="10"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ScrollBar">
                                <Grid x:Name="GridRoot" Background="Transparent">
                                    <Track x:Name="PART_Track" IsDirectionReversed="False">
                                        <Track.DecreaseRepeatButton>
                                            <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageLeftCommand"/>
                                        </Track.DecreaseRepeatButton>
                                        <Track.Thumb>
                                            <Thumb Style="{StaticResource ScrollBarThumb}"/>
                                        </Track.Thumb>
                                        <Track.IncreaseRepeatButton>
                                            <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageRightCommand"/>
                                        </Track.IncreaseRepeatButton>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Loading Spinner Animation -->
        <Storyboard x:Key="SpinnerAnimation" RepeatBehavior="Forever">
            <DoubleAnimation
                Storyboard.TargetName="LoadingSpinner"
                Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                From="0" To="360"
                Duration="0:0:1"/>
        </Storyboard>

        <!-- ToolTip Style for Dark Theme -->
        <Style TargetType="ToolTip">
            <Setter Property="Background" Value="#09090B"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToolTip">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Icon Path Styles -->
        <Style x:Key="IconSmall" TargetType="Path">
            <Setter Property="Width" Value="12"/>
            <Setter Property="Height" Value="12"/>
            <Setter Property="Stretch" Value="Uniform"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Fill" Value="{StaticResource TextSecondaryBrush}"/>
        </Style>

        <Style x:Key="IconSmallSubtle" TargetType="Path">
            <Setter Property="Width" Value="12"/>
            <Setter Property="Height" Value="12"/>
            <Setter Property="Stretch" Value="Uniform"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Fill" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="Opacity" Value="0.6"/>
            <Setter Property="Margin" Value="0,0,4,0"/>
        </Style>

        <Style x:Key="IconMedium" TargetType="Path">
            <Setter Property="Width" Value="14"/>
            <Setter Property="Height" Value="14"/>
            <Setter Property="Stretch" Value="Uniform"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Fill" Value="{StaticResource TextPrimaryBrush}"/>
        </Style>

        <Style x:Key="IconLarge" TargetType="Path">
            <Setter Property="Width" Value="16"/>
            <Setter Property="Height" Value="16"/>
            <Setter Property="Stretch" Value="Uniform"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Fill" Value="{StaticResource TextPrimaryBrush}"/>
        </Style>

        <!-- Mod Card Style -->
        <Style x:Key="ModCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#27272A"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="ClipToBounds" Value="True"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3F3F46"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Filter Bar -->
        <Border Grid.Row="1"
                Background="{StaticResource SurfaceBrush}"
                Padding="16,8"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,0,1">
            <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <!-- Text Filter -->
                <TextBox Width="160" Height="32"
                         Margin="4"
                         Text="{Binding TextFilter, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <VisualBrush Stretch="None" AlignmentX="Left">
                                                <VisualBrush.Visual>
                                                    <TextBlock Text="Search..." Foreground="#52525B" FontStyle="Italic" Padding="10,0,0,0"/>
                                                </VisualBrush.Visual>
                                            </VisualBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <!-- Order By Dropdown -->
                <ComboBox Width="160" Height="32"
                          Margin="4,4,0,4"
                          ItemsSource="{Binding OrderByOptions, Mode=OneTime}"
                          SelectedValue="{Binding OrderBy}"
                          SelectedValuePath="Key">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Path Data="{Binding Icon, Converter={StaticResource IconKeyToGeometry}}" 
                                      Fill="{StaticResource TextSecondaryBrush}" 
                                      Width="12" Height="12" Stretch="Uniform"
                                      VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <TextBlock Text="{Binding Display}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <!-- Order Direction Toggle -->
                <Button Width="32" Height="32"
                        Margin="2,4,4,4"
                        Style="{StaticResource IconButtonStyle}"
                        Command="{Binding ToggleOrderDirectionCommand}"
                        ToolTip="Toggle sort direction">
                    <Grid>
                        <Path Data="{StaticResource IconChevronDown}" 
                              Fill="{StaticResource TextPrimaryBrush}" 
                              Width="10" Height="10" Stretch="Uniform">
                            <Path.Style>
                                <Style TargetType="Path">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding OrderByDirection}" Value="asc">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Path.Style>
                        </Path>
                        <Path Data="{StaticResource IconChevronUp}" 
                              Fill="{StaticResource TextPrimaryBrush}" 
                              Width="16" Height="16" Stretch="Uniform">
                            <Path.Style>
                                <Style TargetType="Path">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding OrderByDirection}" Value="asc">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Path.Style>
                        </Path>
                    </Grid>
                </Button>

                <!-- Versions Filter using custom multi-select -->
                <views:MultiSelectDropdown Width="160" Height="32"
                                           Margin="4"
                                           ItemsSource="{Binding AvailableVersions}"
                                           SelectedItems="{Binding SelectedVersions}"
                                           DisplayMemberPath="Name"
                                           PlaceholderText="Versions"
                                           ToolTip="Select game versions to filter"/>

                <!-- Tags Filter using custom multi-select -->
                <views:MultiSelectDropdown Width="120" Height="32"
                                           Margin="4"
                                           ItemsSource="{Binding AvailableTags}"
                                           SelectedItems="{Binding SelectedTags}"
                                           DisplayMemberPath="Name"
                                           PlaceholderText="Tags"
                                           ToolTip="Select tags to filter"/>

                <!-- Side Filter -->
                <ComboBox Width="120" Height="32"
                          Margin="4"
                          SelectedValue="{Binding SelectedSide}"
                          SelectedValuePath="Key"
                          DisplayMemberPath="Value"
                          ItemsSource="{Binding SideOptions, Mode=OneTime}"/>

                <!-- Installed Filter -->
                <ComboBox Width="120" Height="32"
                          Margin="4"
                          SelectedValue="{Binding SelectedInstalledFilter}"
                          SelectedValuePath="Key"
                          DisplayMemberPath="Value"
                          ItemsSource="{Binding InstalledFilterOptions, Mode=OneTime}"/>

                <!-- Favorites Toggle -->
                <Button Width="32" Height="32"
                        Margin="4"
                        Command="{Binding ToggleFavoriteFilterCommand}"
                        ToolTip="Show only favorites"
                        Style="{StaticResource IconButtonStyle}">
                    <Path Data="{StaticResource IconStarSolid}"
                          Width="16" Height="16" Stretch="Uniform" VerticalAlignment="Center">
                        <Path.Style>
                            <Style TargetType="Path">
                                <Setter Property="Fill" Value="{StaticResource TextSecondaryBrush}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding OnlyFavorites}" Value="True">
                                        <Setter Property="Fill" Value="#FBBF24"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Path.Style>
                    </Path>
                </Button>

                <!-- Clear Filters -->
                <Button Width="32" Height="32"
                        Margin="4"
                        Style="{StaticResource IconButtonStyle}"
                        Command="{Binding ClearFiltersCommand}"
                        ToolTip="Clear all filters">
                    <Path Data="{StaticResource IconXCircle}" 
                          Style="{StaticResource IconLarge}"/>
                </Button>
                <Button Width="32" Height="32" Margin="4"
                            Style="{StaticResource IconButtonStyle}"
                            Command="{Binding SearchModsCommand}"
                            IsEnabled="{Binding IsSearching, ConverterParameter=invert, Converter={StaticResource BoolToVisibility}}"
                            ToolTip="Refresh">
                    <Path Data="{StaticResource IconRefresh}" 
                              Style="{StaticResource IconLarge}"/>
                </Button>
                <Button Width="32" Margin="4" Height="32" Click="ScrollToTop_Click" ToolTip="Scroll to top"  Style="{StaticResource IconButtonStyle}">
                    
                        <Path Data="{StaticResource IconArrowToTop}" 
              Style="{StaticResource IconMedium}"/>

                    
                </Button>
            </WrapPanel>
        </Border>

        <!-- Mod Grid -->
        <ScrollViewer Grid.Row="2"
                      x:Name="ModsScrollViewer"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      ScrollChanged="ModsScrollViewer_ScrollChanged">
            <Grid>
                <!-- Loading Indicator -->
                <StackPanel Visibility="{Binding IsSearching, Converter={StaticResource BoolToVisibility}}"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                    <!-- Loading Spinner Circle -->
                    <Border Width="48" Height="48"
                            x:Name="LoadingSpinner"
                            RenderTransformOrigin="0.5,0.5">
                        <Border.RenderTransform>
                            <RotateTransform Angle="0"/>
                        </Border.RenderTransform>
                        <Ellipse Width="48" Height="48"
                                 Stroke="{StaticResource AccentBrush}"
                                 StrokeThickness="4"
                                 StrokeDashArray="3 2"
                                 StrokeEndLineCap="Round"/>
                    </Border>
                    <TextBlock Text="Loading mods..."
                               FontSize="14"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               TextAlignment="Center"
                               Margin="0,12,0,0"/>
                </StackPanel>

                <!-- No Results -->
                <TextBlock Text="No mods match your filters"
                           FontSize="18"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center">
                    <TextBlock.Visibility>
                        <MultiBinding Converter="{StaticResource NoResultsVisibility}">
                            <Binding Path="IsSearching"/>
                            <Binding Path="ModsList.Count"/>
                        </MultiBinding>
                    </TextBlock.Visibility>
                </TextBlock>

                <!-- Mods Grid -->
                <ItemsControl ItemsSource="{Binding VisibleMods}"
                              Margin="16"
                              Visibility="{Binding ModsList.Count, Converter={StaticResource CountToVisibility}}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"
                                       HorizontalAlignment="Center"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border x:Name="ModCardBorder"
                                    Style="{StaticResource ModCardStyle}"
                                    Width="288"
                                    Margin="8">
                                <Border.Tag>
                                    <Binding Path="ModId"/>
                                </Border.Tag>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <!-- Mod Image -->
                                    <Border Grid.Row="0" Height="192" CornerRadius="7,7,0,0" ClipToBounds="True">
                                        <Grid>
                                            <Image Source="{Binding LogoUrl}"
                                                   Stretch="UniformToFill"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"/>

                                            <!-- Overlay Buttons -->
                                            <StackPanel Orientation="Horizontal"
                                                        HorizontalAlignment="Left"
                                                        VerticalAlignment="Top"
                                                        Margin="8">
                                                <Button Width="28" Height="28"
                                                        Opacity="0.9"
                                                        Click="FavoriteButton_Click"
                                                        Tag="{Binding ModId}"
                                                        ToolTip="Toggle favorite"
                                                        Style="{StaticResource IconButtonStyle}">
                                                    <Button.Visibility>
                                                        <MultiBinding Converter="{StaticResource FavoriteButtonVisibility}">
                                                            <Binding Path="IsMouseOver" ElementName="ModCardBorder"/>
                                                            <Binding Path="ModId"/>
                                                            <Binding Path="DataContext.FavoriteMods" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                                        </MultiBinding>
                                                    </Button.Visibility>
                                                    <Path Data="{StaticResource IconStarSolid}"
                                                          Width="14" Height="14" Stretch="Uniform" VerticalAlignment="Center">
                                                        <Path.Style>
                                                            <Style TargetType="Path">
                                                                <Setter Property="Fill" Value="{StaticResource TextSecondaryBrush}"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Value="True">
                                                                        <DataTrigger.Binding>
                                                                            <MultiBinding Converter="{StaticResource ContainsConverter}">
                                                                                <Binding Path="ModId"/>
                                                                                <Binding Path="DataContext.FavoriteMods" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                                                            </MultiBinding>
                                                                        </DataTrigger.Binding>
                                                                        <Setter Property="Fill" Value="#FBBF24"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Path.Style>
                                                    </Path>
                                                </Button>
                                            </StackPanel>
                                        </Grid>
                                    </Border>

                                    <!-- Mod Info -->
                                    <Grid Grid.Row="1" Height="96">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Title & Description (now on left) -->
                                        <StackPanel Grid.Column="0" Margin="8">
                                            <!-- MaxWidth: Card=288 - StatsColumn(80+16)=96 - Separator=1 - Margins=16 = 175 -->
                                            <StackPanel Orientation="Horizontal" MaxWidth="175">
                                                <!-- MaxWidth: 175 - LinkIcon(10+4margin) = 161 -->
                                                <TextBlock Foreground="{StaticResource TextPrimaryBrush}"
                                                           FontWeight="Bold"
                                                           FontSize="14"
                                                           TextTrimming="CharacterEllipsis"
                                                           MaxWidth="161"
                                                           Cursor="Hand"
                                                           MouseLeftButtonDown="OpenInBrowserTitle_Click"
                                                           Tag="{Binding AssetId}"
                                                           ToolTip="Open in browser">
                                                    <TextBlock.Text>
                                                        <Binding Path="Name"/>
                                                    </TextBlock.Text>
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Style.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter Property="TextDecorations" Value="Underline"/>
                                                                </Trigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                                <Path Data="{StaticResource IconLinkAlt}" 
                                                      Width="10" Height="10" Stretch="Uniform"
                                                      Fill="{StaticResource TextSecondaryBrush}"
                                                      VerticalAlignment="Center"
                                                      Margin="4,0,0,0"/>
                                            </StackPanel>
                                            <TextBlock Text="{Binding Summary}"
                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                       FontSize="12"
                                                       TextWrapping="Wrap"
                                                       MaxHeight="60"
                                                       TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>

                                        <!-- Separator -->
                                        <Border Grid.Column="0"
                                                Width="1"
                                                Background="{StaticResource BorderBrush}"
                                                HorizontalAlignment="Right"/>

                                        <!-- Stats (now on right) -->
                                        <StackPanel Grid.Column="1"
                                                    Margin="8"
                                                    Width="80">
                                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                                <Path Data="{StaticResource IconUser}" 
                                                      Style="{StaticResource IconSmallSubtle}"/>
                                                <!-- MaxWidth: StatsWidth=80 - Icon(12+4margin) = 64 -->
                                                <TextBlock Text="{Binding Author}"
                                                           Foreground="{StaticResource TextPrimaryBrush}"
                                                           FontSize="12"
                                                           MaxWidth="64"
                                                           TextTrimming="CharacterEllipsis"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                                <Path Data="{StaticResource IconDownload}" 
                                                      Style="{StaticResource IconSmallSubtle}"/>
                                                <TextBlock Text="{Binding FormattedDownloads}"
                                                           Foreground="{StaticResource TextPrimaryBrush}"
                                                           FontSize="12"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                                <Path Data="{StaticResource IconHeartSolid}" 
                                                      Style="{StaticResource IconSmallSubtle}"/>
                                                <TextBlock Text="{Binding FormattedFollows}"
                                                           Foreground="{StaticResource TextPrimaryBrush}"
                                                           FontSize="12"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                                <Path Data="{StaticResource IconMessage}" 
                                                      Style="{StaticResource IconSmallSubtle}"/>
                                                <TextBlock Text="{Binding FormattedComments}"
                                                           Foreground="{StaticResource TextPrimaryBrush}"
                                                           FontSize="12"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Grid>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
