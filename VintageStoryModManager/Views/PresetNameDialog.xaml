<Window x:Class="VintageStoryModManager.Views.PresetNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Save Preset"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        MinWidth="320"
        MinHeight="160">
    <Border Padding="16" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
        <StackPanel>
            <TextBlock Text="Enter a name for the preset:" Margin="0,0,0,8" />
            <TextBox x:Name="PresetNameTextBox"
                     MinWidth="260"
                     Margin="0,0,0,12"
                     TextChanged="PresetNameTextBox_OnTextChanged" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CancelButton"
                        Content="Cancel"
                        Width="80"
                        Margin="0,0,8,0"
                        IsCancel="True"
                        Click="CancelButton_OnClick" />
                <Button x:Name="SaveButton"
                        Content="Save"
                        Width="80"
                        IsDefault="True"
                        Click="SaveButton_OnClick" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
